#!/bin/sh
ROOT="${SUNEEL_WORKSPACE:-$HOME/SuneelWorkSpace}"
python3 "$ROOT/scripts/system_intelligence.py" improve "$@"
STATUS="$?"
python3 "$ROOT/anticipation/prediction_engine.py" record --command improve-system --context system --exit-code "$STATUS" 2>/dev/null || true
exit "$STATUS"
