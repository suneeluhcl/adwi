#!/bin/sh
# Create an iMessage draft (no send). Usage: imsg-draft "+15551234567" "message text"
ROOT="${SUNEEL_WORKSPACE:-$HOME/SuneelWorkSpace}"
exec python3 "$ROOT/comms/imessage/scripts/imessage-send-draft" "$@"
