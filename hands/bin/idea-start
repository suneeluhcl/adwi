#!/bin/sh
ROOT="${SUNEEL_WORKSPACE:-$HOME/SuneelWorkSpace}"
python3 "$ROOT/research-engine/research_engine.py" capture "$@"
STATUS="$?"
python3 "$ROOT/anticipation/prediction_engine.py" record --command idea-start --context research --exit-code "$STATUS" 2>/dev/null || true
exit "$STATUS"
