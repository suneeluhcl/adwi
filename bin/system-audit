#!/bin/sh
ROOT="${SUNEEL_WORKSPACE:-$HOME/SuneelWorkSpace}"
python3 "$ROOT/scripts/system_intelligence.py" audit "$@"
STATUS="$?"
python3 "$ROOT/anticipation/prediction_engine.py" record --command system-audit --context system --exit-code "$STATUS" 2>/dev/null || true
exit "$STATUS"
