# Code Improvement Workflow

This workflow searches for a best coding approach, reads a file, proposes a fix, and logs an issue.

## Steps
1. search_web_brave "best python AST parsing libraries"
2. filesystem_read_file "scripts/integrity_guard.py"
3. generate_fix
4. github_create_issue "Upgrade integrity guard AST parsing"
